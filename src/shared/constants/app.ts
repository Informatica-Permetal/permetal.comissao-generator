export const APP_NAME = 'Formatador Comissão';

/** Must match `build.appId` in package.json - stamped into the report-root manifest so a future version can recognize its own data. */
export const APP_ID = 'com.formatadorcomissao.app';
